---
mdoc: true
theme: system
title: "Introduction (standalone preview)"
---

# Introduction

The introduction is its own file. It carries a little frontmatter so that
opening it directly gives a sensible standalone preview — that frontmatter is
discarded when the chapter is included from the root.

## Background {#background}

This section is labelled with an explicit `{#background}` id so a later chapter
can refer back to it by number.
