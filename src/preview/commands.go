package preview

import "encoding/json"

func (s *PreviewServer) handleSave(payload json.RawMessage) error {
	return nil
}

func (s *PreviewServer) handleSaveAt(payload json.RawMessage) error {
	return nil
}
